# Общая информатика
© Бибиков Сергей, к.т.н., доцент, кафедра суперкомпьютеров и общей информатики, Самарский университет

## Список полезных ссылок для курса (обновляемый)

<http://cppstudio.com/> - Основы программирования на языках Си и C++ для начинающих. Если вы чего-то не понимаете, это хороший сайт для того чтобы самостоятельно закрыть пробелы в знаниях.

<https://www.onlinegdb.com/online_c++_compiler> - Онлайн компилятор C++. Вам не обязательно устанавливать среду программирования для выполнения заданий.

<https://github.com/> - сайт на котром мы находимся, используется для контроля версий, полезно для различных программных прикладов и документов общего пользования.

## Правила игры

__Структура курса:__

__Лекции__ - 12 часов - 6 занятий - даты занятий 14.02, 28.02, 14.03, 28.03, 11.04, 25.04. Лекционный материал я буду выкладывать в своем репозитории накануне занятия.
Что будет на лекциях: базовые вещи для программирования на С++; концепции и способы мышления; простые примеры кода; домашние задания; индивидуальные задания; задания повышенной сложности.

Буду отмечать и периодически дергать во время лекции. Буду выборочно спрашивать по поводу домашнего задания. Вопросы задавать можно и нужно, прямо в процессе. Глупых вопросов не бывает, но бывают умные вопросы. Активность, вопросы, полезные замечания и всякое такое я буду отмечать тоже.

__Лабораторные работы__ - 18 часов на каждую подгруппу - 4,5 лабораторные работы - даты занятий: 1 подгруппа: 25.02, 25.03, 22.04, 20.05; 2 подгруппа: 11.03, 08.04, 06.05, 03.06. Резервная лабораторная работа - 17.06.
Что будет на лабораторных работах: раздача вариантов; обсуждение особенностей выполнения заданий; ответы на вопросы связанные с практическим программированием; сдача заданий.

Буду обязательно отмечать. 

__Самостоятельная работа__ - 36 часов.
Что входит в самостоятельную работу: изучение источников; выполнение домашних заданий; выполнение индивидуального задания.

__Экзамен__

Первое правило автомата - не спрашивать про автомат. Автомат - способ сэкономить мое время на экзамене, не расчитывайте на него. Если вы умеете программировать, то вы его получите, скорее всего. Все заслуги будут учитываться, но они ничто без реальных знаний.

Допуск к экзамену: все лабораторные работы.

Из чего состоит экзамен: 

1. 1 задача по написанию программы. Обязательная часть.
2. 2 микропримера на практические знания С++.
3. 1 теоретический вопрос.


## Лекция 1

### Что такое программа?

Компьютерная программа — 1) комбинация компьютерных инструкций и данных, позволяющая аппаратному обеспечению вычислительной системы выполнять вычисления или функции управления; 2) синтаксическая единица, которая соответствует правилам определённого языка программирования, состоящая из определений и операторов или инструкций, необходимых для определённой функции, задачи или решения проблемы.

Человеческим языком: 

_Программа_ - комбинация инструкций, написанных на языке программирования, понятном компилятору, и преобразуемая им в комбинацию инструкций, понятных исполнителю.

_Компилятор_ - специальная программа переводящая текст, написанный на языке программирования, в машинные коды.

_Исполнителем_ в данном случае является процессор.

Пример для понимания: заказчик составляет техническое задание, которое способен понять инженер-конструктор; конструктор разрабатывает чертеж, понятный фрезеровщику; фрезеровщик на станке изготавливает деталь. В данном случае роль компилятора выполняет инженер-конструктор.

### Компиляция и интерпретация

Есть несколько видов реализации языков программирования, нам понадобится представление о двух из них: компиляция и интерпретация.

Компиляция подразумевает полное преобразование исходного кода (того самого текста на языке программирования) в машинный код. Плюсы: быстродействие, эффективность, возможность автоматической оптимизации.

Интерпретация подразумевает пошаговое выполнение инструкций исходного кода непосредственно во время вызова. Плюсы: гибкость использования языка.

Разделение это, если что, достаточное условное. Но нам важно знать, что С++ компилируемый язык. Для выполнения программы она должна быть написана вся целиком и компилироваться без ошибок.

Популярным представителем интерпретируемого языка является Python. Программирование можно прекрасно учить и на нем. Так было бы даже легче, но С++ вырабатывает дисциплину, то что нужно на первом курсе.

Вопрос: если компилятор это программа, то на каком языке программирования был написан первый компилятор и как можно было скомпиллировать исходный код для первого компилятора?

### Структура программы

Вспомним понятие функции из математики. 

_y = sin(x)_ - функция синуса.

Здесь _x_ - аргумент функции, то что передается внутрь для вычисления;

_sin_ - название функции, соответствует правилам, по которым обрабатывается _x_;

_y_ - величина которая будет равна посчитанному значению.

В С++ все описывается похожим образом и имеет те же названия: аргумент функции, имя функции. Функция вызывается в тот момент, когда необходимо произвести вычисления, заложенные в функцию, для продолжения работы.

Для правильной работы функции аргумент(ы) для ее вызова должны быть известны. После окончания работы функция возвращает (или нет, если не предусмотрено программистом) какое-то значение.

Функция может выполнять какие-то действия, не обязательно математические. Функция может работать и без аргументов, если так задумано.

Если фам в работе понадобиласть какая-то функция (например математическая), то совсем необязательно ее писать самому. Скорее всего она уже написана до вас, и написана хорошо. Функции схожие по тематике объединяют в специальные сборники - библиотеки. Чтобы не пришлось изобретать велосипед, достаточно подключить соответствующую библиотеку.

Как оформляются функции в C++ (максимально упрощенный вариант без подробностей)

```
<ТИП ДАННЫХ ВОЗВРАЩАЕМОГО ЗНАЧЕНИЯ> <ИМЯ ФУНКЦИИ>(<ТИП ДАННЫХ АРГУМЕНТА><ВНУТРЕННЕЕ ИМЯ АРГУМЕНТА>, ...){
  <ТЕЛО ФУНКЦИИ>
  <ВОЗВРАТ ЗНАЧЕНИЯ>
}
```
`<ТИП ДАННЫХ ВОЗВРАЩАЕМОГО ЗНАЧЕНИЯ>` и `<ТИП ДАННЫХ АРГУМЕНТА>` - определяет, какого рода данные мы используем, мы об этом поговорим позже

`<ИМЯ ФУНКЦИИ>` - то название под которым мы сможем вызвать функцию в программе или даже в другой функции

`<ВНУТРЕННЕЕ ИМЯ АРГУМЕНТА>` - имя, под которым аргумент применяется внутри функции

`<ТЕЛО ФУНКЦИИ>` - тут перечисляются все инструкции/команды, которые функция должна выполнить во время своей работы, например для вычисления конечного значения

`<ВОЗВРАТ ЗНАЧЕНИЯ>` - процедура явного возврата функцией значения в место вызова

__Пример:__

```c++
int sum_of_xyz(int a, int b, int c){
  int result = a + b + c;
  return result;
}
```

__Пример:__

```c++
int dummy(){
  return 0;
}
```

При этом вызов таких написанных нами функций будет выглядеть очень похоже на математическую запись:


```c++
int y;
y = sum_of_xyz(1, 2, 14);
dummy();
```

Теперь вы представляете как выглядит упрощенное описание функции в C++. Зачем это нужно, ведь еще ничего не понятно? Дело в том, что программа на языке C++ это тоже функция. Главная функция, которая внутри себя вызывает другие функции. И оформляется она точно так же как и остальные функции.


```c++
int main()
{
  return 0;
}
```

Основной особенностью такой _главной функции_ является ее имя, оно должно быть __main__. У главной функции могут быть аргументы, их может не быть (как в примере). Это определяется способом запуска вашей программы, особенностями среды разработки (той программы, в которой вы пишете свои программы).

Когда вам понадобится подключить библиотеку с необходимыми функциями, а вам точно понадобится, нужно будет использовать следующую запись.

```c++
#include <имя библиотеки>
```

__Пример:__

```c++
#include <iostream>
```

Как именно называется нужная вам библиотека можно узнать в сети.

Иногда появляется необходимость написать прямо в исходном коде текст, который не должен рассматриваться компилятором, как инструкция. Например, текст комментария на русском языке. Для этого существует два способа. Закомментировать одну строчку (ее часть) или закомментировать целый блок текста.

Комментриование строки или ее части можно поставив `//`. После этих символов и до конца строки все написанное будет игнорироваться компилятором и не считаться программой.
Комментирование блока текста достигается использованием `/*` и `*/` для указания начала и конца комментируемой области.

__Пример__

```c++
//Отдельная строка с комментарием
a = b; //Этот способ позволяет оставить комментарий напротив нужного места без выделения лишней строки
/* Тут можно закомментировать целый блок из нескольких строчек
a = b + 1;
b = a + 1; */
```

Используя комментарии можно "выключать" часть исходного кода, которы вы не хотите стирать. Никогда не стирайте работающий код, лучше его комментировать. Когда программа будет полностью работоспособна, можно будет навести красоту.

Еще одно важная вещь, которую вы уже могли заметить. Каждая инструкция в C++ заканчивается точкой с запятой ";".

### Переменные

Исторически программирование родилось из необходимости проводить математические вычисления. Отсюда и сильное влияние концепций математики на все программирование.
Для того, чтобы произвести операции с числами, эти числа нужно как-то хранить внутри программы.

Наивное решение - напрямую задавать нужные значения прямо в исходном коде. Так вначале и было. Все числа для расчетов задавались сразу в тексте программы. Затем проходила компиляция и вычисления. Оказалось не очень удобно. Особенно, если числа приходилось менять часто. А программа вычислений при этом не менялась.

Для этого стали использовать оперативную память, чтобы можно было "запоминать" и "вспоминать" числа (данные) в процессе выполнения программы.
Концепция простая: есть ячейчки физической оперативной памяти, которые могут хранить закодированные в бинарном виде данные. У этих ячеек есть адреса, по которым можно к ним обратиться для записи или считывания информации. У программиста есть имена, под которыми он хотел бы использовать данные. Система связывает имена для разных данных, используемые программистом, и адреса ячеек, в которых эти данные хранятся в оперативной памяти. Данные при этом бывают разные и могут не умещаться в одну ячейку. Поэтому кроме адреса первой ячейки нужно знать и количество ячеек, используемых для хранения одного элемента данных (числа, символа, строки и т.д.).

Поэтому для того, чтобы вычислительная система "знала", как работать с тем или иным куском данных, необходимо обозначить тип данных.

Например, если нужно посчитать сумму двух целых чисел _a_ и _b_ и записать результат как _c_, то нужно "предупредить" компилятор о всех необходимых нам ячейках.

```c++
int a, b, c;
```

Это "предупреждение" называется __объявление переменных__. Переменные потому, что в ячейке мы можем хранить в процессе выполнения разные значения. Компилятор преобразует запись с объявлением переменных в набор команд, которые при запуске программы перед началом расчетов выделят необходимое количество ячеек памяти для хранения трех целых (int - integer - целое) чисел; привяжут соответствующие адреса к именам _a_, _b_ и _с_ из текста программы; и каждый раз когда в программе будет использоваться для вычислений одно из этих чисел, копировать из ячеек необходимые значения и предоставлять их в место вызова.

Но только объявить пересенные мало. Само объявление это только подготовка места для хранения. Нужно еще и заполнить эти места значениями. Это называется __инициализация переменных__. Без инициализации каким-либо значением переменные не могут быть использованы, так как не имеют смысла. 

Инициализацию можно сделать сразу при объявлении или позже:

```c++
int a = 10, b, с;
b = 7;
c = a + b;
```

Можно значения считать из файла, ввести с клавиатуры, даже снять с камеры телефона.

В C++ используется множество типов данных. Основными, которые мы будем использовать в рамках курса будут:

__Целочисленные типы:__

`[unsigned][short][long] int` - используются для хранения целых чисел в разных диапазонах, выход за диапазон не предусмотрен. Варианты позволяют учитывать или не учитывать знак числа, уменьшать или увеличивать количество занимаемой памяти (с уменьшением и увеличением диапазона, соответственно).

`char` - используются для хранения символов (по факту являются номером хранимого символа в специальной __стандартной__ таблице).

`bool` - используются для хранения логических значений `true` и `false`. Да такой тип тоже считается челочисленным, так как соответствует двум целым значениям 1 и 0.

__Числа с плавающей точкой:__

`float` и `double` - используются для хранения дробных чисел. Первое поменьше диапазоном и точностью, второе побольше.

Особенности работы с типами данных будут одной из тем лабораторной работы.

### Ввод и вывод (минимальный обзор)

Для работы часто необходимо вводить данные для расчетов с клавиатуры и выводить результаты на экран. Для этих целей создан мощный инструментарий, собранный в библиотеку __iostream__. Минимальным обязательным для нас набором из этой библиотеки являются `cin` и `cout`.

`cin` позволяет вводить значения с клавиатуры. Синтаксис следующий

```c++
cin >> a;
```

при выполнении этой строки программа будет ждать, пока вы не введете что-либо при помощи клавиатуры и не нажмете `enter`. После этого введенные символы будут преобразованы (успешно или нет) к типу данных, указанному для `a`.

`cout` позволяет выводить на экран значения переменных, текст, различные символы. Синтаксис операции вывода на экран

```c++
cout << a; // вывод значения, хранящегося в a
cout << 'R'; // вывод символа английского алфавита R (кавычки одинарные)
cout << "example"; // вывод текста example (двойные кавычки);
cout << "a = " << a; // Вопрос: что здесь выводится на экран?
```
Для вывода символов, которые могут компилятором восприниматься как часть исходного кода (например "), применяются специальные последовательности

`\r`	возврат каретки в начало строки

`\n`	новая строка

`\t`	горизонтальная табуляция

`\v`	вертикальная табуляция

`\»`	двойные кавычки

`\’`	апостроф

`\\`	обратный слеш

`\0`	нулевой символ

`\?`	знак вопроса

и др.

Для перехода на следущую строку в `cout` можно использовать endl - end of line - конец строки

```
cout << "1st line" << endl << "2nd line";
```

Теперь можно записать простейшую и чуть более сложную программу на языке C++, которую вы сможете прочитать и понять.

__Пример__

```c++
#include <iostream> //здесь подключается библиотека для ввода и вывода

using namespace std; // пока это волшебная фраза, значение которой расскажу позже
 
int main() // тип возвращаемого значения основной функции
{ // начало тела программы
    cout << "My first program!" << endl; // вывод на экран фразы "My first program!" с переходом на следующую строку
    system("pause"); // служебная команда, заставляющая систему ждать нажатия кнопки на клавиатуре, используется для задержки экрана, чтобы пользователь смог прочесть текст
    return 0; // точка возврата
} // конец тела программы
```

__Пример__


```c++
#include <iostream> // здесь подключается библиотека для ввода и вывода
#include <iostream> // здесь подключается библиотека математических функций

using namespace std; // пока это волшебная фраза, значение которой расскажу позже
 
int main() // тип возвращаемого значения основной функции
{ // начало тела программы
    int a = 10; // объявляем и инициализируем целочисленную переменную 
    double b, c; // объявляем как числа с плавающей точкой две переменные
    cin >> b; // вводим значение с клавиатуры и инициализируем им переменную 
    c = sqrt(b) + a; // вычисляем с применением математической функции корня значение и инициализируем им переменную
    cout << "c = sqrt(b) + a = " << c << endl; // вывод на экран фразы "My first program!" с переходом на следующую строку
    system("pause"); // служебная команда, заставляющая систему ждать нажатия кнопки на клавиатуре, используется для задержки экрана, чтобы пользователь смог прочесть текст
    return 0; // точка возврата
} // конец тела программы
```
__Пример__


```c++
#include <iostream> // здесь подключается библиотека для ввода и вывода
#include <iostream> // здесь подключается библиотека математических функций

using namespace std; // пока это волшебная фраза, значение которой расскажу позже

double my_function(double x, int y) // объявляем нашу функцию типа double с двумя аргументами типа double и int
{ // начало тела функции
  double result; // объявляем переменную для хранения результата
  result = sqrt(x) + y; // вычисляем результат с использованием математической функции sqrt (корень числа)
  return result; // возвращаем вычисленный результат
} // конец тела функции

int main() // тип возвращаемого значения основной функции
{ // начало тела программы
    int a = 10; // объявляем и инициализируем целочисленную переменную 
    double b, c; // объявляем как числа с плавающей точкой две переменные
    cin >> b; // вводим значение с клавиатуры и инициализируем им переменную 
    c = my_function; // вычисляем с вызовом созданной функции значение и инициализируем им переменную c
    cout << "c = sqrt(b) + a = " << c << endl; // вывод на экран фразы "My first program!" с переходом на следующую строку
    system("pause"); // служебная команда, заставляющая систему ждать нажатия кнопки на клавиатуре, используется для задержки экрана, чтобы пользователь смог прочесть текст
    return 0; // точка возврата
} // конец тела программы
```

## Конец Лекции 1


## Первое домашнее задание: 
1. Прочитать в сети, что такое git-системы, зачем они нужны, что такое репозиторий, что такое markdown.
2. Создать себе акаунт на GitHub
3. Создать в репозитории папку "ОИ ЛР" и создать 4 пустых файла в формате markdown для отчетов по ЛР (имена можно задать "ЛР_1.md","ЛР_2.md","ЛР_3.md","ЛР_4.md").
4. Прислать мне в телеграм чат ссылку на ваш репозиторий, чтобы я внес информацию в гугл-таблицу.
